FROM python:3.14.2-slim
WORKDIR /app
COPY baked_blog-main/requirements.txt .
RUN apt-get update && apt-get install -y \
    zlib1g-dev \
    libjpeg-dev \
    gcc
RUN pip install -r requirements.txt 
COPY . .
CMD ["python","manage.py","runserver","0.0.0.0:8000"]

